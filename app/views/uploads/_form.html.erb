<!-- Form for uploading  a new resource -->
<!--form_for call uses an array, which will build a nested route, such as /flashcards/1/uploads -->
<%= form_for([@flashcard, @flashcard.uploads.build]) do |form| %>
  <%= form.file_field :upload %>
  <%= form.submit "Save File" %>
<% end %>